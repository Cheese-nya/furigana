# 🎌 Furigana Dubbing Studio V1.0.1 发布说明 (Release Notes)

> **版本标签 (Tag)**: `v1.0.1`  
> **发布标题 (Title)**: `Furigana V1.0.1 — 上下文消歧重构、动态台本标题与极简本地化`  
> **发布日期 (Date)**: 2026-09-19  

---

## 🌟 核心更新亮点 (Highlights)

### 1. 🎯 上下文（前后文）假名高精度消歧引擎
- **根除分词破坏 Bug**：形态素分析全面切换为以上下文 Viterbi 最优路径读音为基准，彻底杜绝单字字典暴力覆盖多音词。
- **双向滑动窗口消歧**：针对常见同形異音語（如 `辛い(からい/つらい)`、`何(なん/なに)`、`角(かど/つの/かく)`、`風(ふう/かぜ)`、`方(かた/ほう)`、`一日(いちにち/ついたち)` 等），结合前后文语义与句法结构自动精准定音。
- **熟字训与送假名精准保护**：优化动词/形容词送假名剥离对齐，`日本人(にっぽんじん)`、`大人(おとな)`、`山田先生(やまだせんせい)` 完美逐字切分。
- **Python 原生模式升级**：同步接入系统 `fugashi` + `unidic_lite` 原生形态素引擎。

### 2. 📝 导出 Word 大标题与导入文件名智能联动
- **文件名自适应标题**：导入文本文件（如 `歪な鳥籠.txt`、`歪な鳥籠.docx`）时，导出的 Word 配音台本一级大标题自动命名为 **「歪な鳥籠」**，彻底告别硬编码的“日语假名配音台本”。
- **台本标题实时编辑**：在台本输入卡片顶部新增 **「台本标题」输入框** 与 **「📂 导入文本」按钮**，方便导入后随时二次自定义。
- **批量处理中心同步**：批量处理队列中每一个任务，导出 Word 文档均独立对应其原始文件的文件名。

### 3. ⚡ 取消 AI 智能注音与翻译，回归极简极速
- **纯粹本地注音**：移除了顶部复杂的“AI 智能注音与翻译”开关、“AI 引擎配置”模态框与依赖外部 API 的校对按钮。
- **净化台本排版**：移除了预览区与导出文件中的机器翻译副行，界面更加清爽专注，配音与台词一目了然。
- **零网络依赖**：全流程毫秒级本地响应，断网环境下依然稳定可靠。

### 4. 🎨 新拟物派 (Neumorphism) 物理触感反馈修复
- **修复导出按钮常凹缺陷**：修复了「导出 Word (.docx)」按钮误用选中态样式导致默认常态凹陷的显示错误；恢复为立体凸起外观，按下凹陷、松开自然回弹。
- **规范全站按钮动作**：批量处理、保存修改、弹窗关闭等操作按钮统一交互反馈规范。

### 5. 🏷️ 新增版本号徽章
- 在左侧抽屉侧边栏底部（作者推特 `チーズ@beimisama` 下方），新增 **`Furigana V1.0.1`** 新拟物凹凸质感版本号徽章。

---

## 📋 详细变更清单 (Detailed Changelog)

### 引擎与核心算法
- 🔄 **[furigana.ts]** 重构形态素注音管道，新增 `disambiguateToken` 与 `parseCompoundToTokens` 算法。
- 🔄 **[furigana_engine.py]** 接入 `fugashi` 原生分词器并同步上下文消歧规则。
- 🔄 **[docxExporter.ts]** `buildDocxBlob` 接入动态 `title` 参数，生成标准 `<w:ruby>` 节点。

### 界面与交互体验
- ➕ **[app/page.tsx]** 新增单文件编辑区 `docTitle` 状态、标题输入框与文件快速导入通道。
- ➖ **[app/page.tsx]** 移除 AI 翻译副行、配置模态框及冗余请求轮询代码。
- 💄 **[app/page.tsx]** 修正「导出 Word (.docx)」按钮样式为 `neuButton`，恢复自然回弹。
- 🏷️ **[app/components/Sidebar.tsx]** 抽屉侧边栏底部加入 `Furigana V1.0.1` 徽章。

### 桌面端与可执行程序
- 📦 重新打包并发布独立的 Windows 原生运行程序 **`FuriganaDubbingStudio.exe`**。

---

## 💾 运行与下载 (Download & Usage)

- **直接运行**：在 Release 附件中下载 `FuriganaDubbingStudio.exe`，双击即可直接在 Windows 运行（免配置 Node/Python 环境）。
- **源码启动**：
  ```bash
  git clone https://github.com/Cheese-nya/furigana.git
  cd furigana
  npm install
  npm run dev
  ```
